name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Get dependencies
        run: |
          cd perler_bead_designer
          flutter pub get

      - name: Build macOS
        run: |
          cd perler_bead_designer
          flutter build macos --release

      - name: Create DMG
        run: |
          # 这一步将文件生成在 build/macos/ 目录下
          cd perler_bead_designer/build/macos/Build/Products/Release
          hdiutil create -volname "BunnyCC Perler" \
            -srcfolder "兔可可的拼豆世界.app" \
            -ov -format UDZO \
            "../../../bunnycc-perler-macos.dmg"

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-release
          # 修改这里：指向 build/macos/ 下的文件
          path: perler_bead_designer/build/macos/bunnycc-perler-macos.dmg
          retention-days: 30

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Get dependencies
        run: |
          cd perler_bead_designer
          flutter pub get

      - name: Build Windows
        run: |
          cd perler_bead_designer
          flutter build windows --release

      - name: Create ZIP archive
        run: |
          # 这一步将文件生成在 build/windows/ 目录下
          cd perler_bead_designer/build/windows/x64/runner/Release
          Compress-Archive -Path * -DestinationPath ../../../bunnycc-perler-windows.zip

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-release
          # 修改这里：指向 build/windows/ 下的文件
          path: perler_bead_designer/build/windows/bunnycc-perler-windows.zip
          retention-days: 30

  create-release:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: macos-release
          path: ./artifacts/

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: windows-release
          path: ./artifacts/

      - name: Get version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: 兔可可的拼豆世界 ${{ steps.get_version.outputs.VERSION }}
          body: |
            ## 🎉 兔可可的拼豆世界 ${{ steps.get_version.outputs.VERSION }}
            
            ### 下载
            - **macOS**: bunnycc-perler-macos.dmg
            - **Windows**: bunnycc-perler-windows.zip
            
            ### 系统要求
            - macOS 10.14 (Mojave) 或更高版本
            - Windows 10 或更高版本
            
            ### 更新内容
            请查看 [更新日志](https://github.com/${{ github.repository }}/blob/main/README.md#-更新日志)
          files: |
            artifacts/bunnycc-perler-macos.dmg
            artifacts/bunnycc-perler-windows.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}